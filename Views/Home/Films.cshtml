@model List<string>
@{
    ViewData["Title"] = "Contact";
}
@if (Model != null)
{
 <div class="text-center">
     <h1>Alle namen!</h1>
     @foreach (string naam in Model)
     {
        <p>@naam</p>
     }
 </div>
}


